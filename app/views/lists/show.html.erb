<h1 class="show-title"><%= @list.name %></h1>

<% @list.product_lists.each do |product_list| %>
  <% if product_list.taken? %>
    <div class="card-product taken">
      <%= product_list.quantity %>
      <% if product_list.quantity.to_i > 1 %>
        <%= "#{product_list.product.name}s" %>
      <% else %>
        <%= product_list.product.name %>
      <% end %>
      <a href=""></a>
    </div>
  <% else%>
    <div class="card-product ">
      <%= product_list.quantity %>
      <% if product_list.quantity.to_i > 1 %>
        <%= "#{product_list.product.name}s" %>
      <% else %>
        <%= product_list.product.name %>
      <% end %>
      <a href=""></a>
    </div>
  <% end %>
<% end %>
<%= render "product_lists/new", locales: [@list, ProductList.new] %>
